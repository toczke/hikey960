name: ARM64 Kernel and DTB Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install Toolchain and Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-aarch64-linux-gnu build-essential bc bison flex libssl-dev make device-tree-compiler

    - name: Fetch Linux Kernel Source
      run: |
        git clone --depth 1 https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git linux-src

    - name: Configure Kernel
      working-directory: ./linux-src
      run: |
        make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- defconfig

    - name: Build Kernel (Image.gz)
      working-directory: ./linux-src
      run: |
        make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- -j$(nproc) Image.gz

    - name: Build Device Tree Blobs (DTB)
      working-directory: ./linux-src
      run: |
        make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- -j$(nproc) dtbs

    - name: Upload Kernel Image
      uses: actions/upload-artifact@v4
      with:
        name: hikey960-kernel-image
        path: linux-src/arch/arm64/boot/Image.gz
        retention-days: 7

    - name: Upload Compiled DTBs
      uses: actions/upload-artifact@v4
      with:
        name: hikey960-dtbs
        path: linux-src/arch/arm64/boot/dts/hisilicon/*.dtb
        retention-days: 7
